@anchor{Function starts-with}
@deffn {Function} {starts-with} object sequence @&key test key
@findex starts-with
Returns true if @code{sequence} is a sequence whose first element is @code{eql} to @code{object}.
Returns @code{nil} if the @code{sequence} is not a sequence or is an empty sequence.
@end deffn

@anchor{Function parse-ordinary-lambda-list}
@deffn {Function} {parse-ordinary-lambda-list} lambda-list @&key normalize allow-specializers normalize-optional normalize-keyword normalize-auxilary
@findex parse-ordinary-lambda-list
Parses an ordinary lambda-list, returning as multiple values:

@code{1}. Required parameters.

@code{2}. Optional parameter specifications, normalized into form:

@lisp
   (name init suppliedp)
@end lisp

@code{3}. Name of the rest parameter, or @code{nil}.

@code{4}. Keyword parameter specifications, normalized into form:

@lisp
   ((keyword-name name) init suppliedp)
@end lisp

@code{5}. Boolean indicating @code{&allow-other-keys} presence.

@code{6}. @code{&aux} parameter specifications, normalized into form

@lisp
   (name init).
@end lisp

@code{7}. Existence of @code{&key} in the lambda-list.

Signals a @code{program-error} is the lambda-list is malformed.
@end deffn
